# 🚀 Setup Instructions

## Quick Start

1. **Install Dependencies**
   ```bash
   npm install
   ```

2. **Start Development Server**
   ```bash
   npm run dev
   ```

3. **Build for Production**
   ```bash
   npm run build
   ```

## 📱 PWA Icons Setup

You need to create PWA icons for the app to work properly. You can:

1. **Use an online tool** like [PWA Asset Generator](https://github.com/onderceylan/pwa-asset-generator)
2. **Create manually**:
   - `public/pwa-192x192.png` - 192x192 pixels
   - `public/pwa-512x512.png` - 512x512 pixels
   - `public/apple-touch-icon.png` - 180x180 pixels

3. **Quick icon creation** (if you have ImageMagick):
   ```bash
   # Create a simple heart icon
   convert -size 512x512 xc:none -fill "#e91e63" -draw "path 'M256,400 C256,400 100,300 100,200 C100,125 150,75 225,75 C256,75 256,100 256,100 C256,100 256,75 287,75 C362,75 412,125 412,200 C412,300 256,400 256,400 Z'" public/pwa-512x512.png
   ```

## 📸 Adding Your Photos

1. Place your photos in `public/uploads/` folder
2. Update the photos array in `src/pages/GalleryPage.jsx` with your photo filenames

## 🎵 Adding Music

1. Add your audio files to `public/` folder
2. Update the music player in `src/pages/HomePage.jsx`:
   ```jsx
   const [audio] = useState(new Audio('/your-song.mp3'))
   
   const toggleMusic = () => {
     if (musicPlaying) {
       audio.pause()
     } else {
       audio.play()
     }
     setMusicPlaying(!musicPlaying)
   }
   ```

## 🎨 Customization

### Change Colors
Edit gradient colors in:
- `src/index.css` - Global styles
- Individual page CSS files

### Update Dates
- `src/pages/HomePage.jsx` - Line 11: Anniversary date
- `src/pages/StoryPage.jsx` - Story events dates

### Update Content
- Edit text in each page component
- Update quotes in `src/pages/HomePage.jsx`
- Update dreams in `src/pages/PromisesPage.jsx`

## 🌐 Deploy

### Vercel
```bash
npm install -g vercel
vercel
```

### Netlify
1. Build the project: `npm run build`
2. Deploy the `dist` folder

### GitHub Pages
1. Install gh-pages: `npm install --save-dev gh-pages`
2. Add to package.json:
   ```json
   "scripts": {
     "deploy": "npm run build && gh-pages -d dist"
   }
   ```
3. Run: `npm run deploy`

## 📝 Notes

- The app uses React Router for navigation
- PWA features work best when served over HTTPS
- Service worker is automatically generated by Vite PWA plugin
- All animations use Framer Motion for smooth performance

